To create Cogue code reference in rst,

% sphinx-apidoc -H "Cogue code reference" -f -o . ./lib/python

Then create HTML files by

% make html

HTML files are created in _build. Copy them to the top directory.

Togo
 
